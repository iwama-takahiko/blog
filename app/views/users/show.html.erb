<div class="container" >
   <div class="pt-5 pb-5 mt-4">
    <% @posts.each do |post| %>
     <%=render partial: "posts/post", locals:{post: post} %>
     <% end %>
  </div>
  <%= paginate(@posts) %>
</div>