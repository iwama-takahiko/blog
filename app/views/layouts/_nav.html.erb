<nav class="navbar navbar-expand-lg navbar-dark bg-info fixed-top ">
<div class="container">

        <a class="navbar-brand col-10" href="/">BLOG</a>
        <% if user_signed_in?  %>

        <div class="collapse navbar-collapse col-2 " id="navbarNav">
             <ul class="navbar-nav">
                <li class="nav-item">
                    <%=link_to current_user.nickname,"/users/#{current_user.id}" ,method: :get,:class =>'btn btn-info text-white nav-link' %>
                    </li>
                <li class="nav-item ">
                <%= link_to 'Logout' ,destroy_user_session_path, method: :delete , :class => "btn btn-info nav-link text-white"%> 
                </li>
                <li class="nav-item">
                <%=link_to 'NEW',new_post_path(current_user.id) ,method: :get, :class =>"btn btn-info nav-link text-white"%>
                </li>
                </div>

            <%else%>
          <div class="collapse navbar-collapse " id="navbarNav col-2">
            <ul class="navbar-nav">
            <li class="nav-item"><%= link_to 'Login', new_user_session_path, :class => 'btn btn-info post nav-link text-white'  %>
            </li>
            <li class="nav-item">
            <%= link_to 'Signup',new_user_registration_path, :class => 'post btn btn-info nav-link text-white'%>
            </li>
            </ul>
          </div>
            <% end %>
            
        </div>
        </div>
    </nav>
